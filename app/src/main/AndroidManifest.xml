<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seatplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".about_me"></activity>
        <activity android:name=".dresult" />
        <activity android:name=".dunit" />
        <activity android:name=".cresult" />
        <activity android:name=".cunit" />
        <activity android:name=".bresult" />
        <activity android:name=".bunit" />
        <activity android:name=".aresult" />
        <activity android:name=".aunit" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>